%h1 Edit #{@topic.title}

.col-md-8
  = form_for @topic do |f|
    .form-group
      = f.label :title
      = f.text_field :title, class: "form-control", placeholder: "#{@topic.title}"
    .form-group
      = f.submit "Save", class: "btn btn-success"
